# 2026-08-07 企业微信工作台免登实现

完成企业微信工作台自建应用免登（OAuth2 snsapi_base）后端改造。决策：自动建号 + 沿用 TokenHelper 加过期 + 前端自取 code 调后端 POST 接口。

## 改动文件（均在 LocalDataApi/LocalDataApi/）
- `Domain/Identity/User.cs`：新增 `WeChatWorkUserId` 字段（企微身份绑定）
- `Utils/TokenHelper.cs`：`CreateToken` 增加 `expiryMinutes`（默认 1440），token 格式改为 `userName|issuedTicks|expiryTicks|sig`；`TryValidate` 校验过期。旧 token 全部失效。
- `Application/Identity/IUserService.cs` + `UserService.cs`：新增 `LoginByWeChatWorkAsync`（code→userid→查绑定→未命中自动建号→复用禁用/锁定风控→登录审计→签发 token）；注入 `IWechatWorkUserService`；`Auth:Secret` 为空时回退默认密钥，读取 `Auth:TokenExpiryMinutes`。
- `Dto/LoginDto.cs`：新增 `WeChatWorkLoginDto{Code}`
- `Api/Controllers/AuthController.cs`：新增 `POST login-by-wechatwork`（返回 `ApiResponse<LoginResultDto>`）
- `Api/Controllers/WechatController.cs`：`authorize-url` 增加 `redirectUri` 白名单校验（`WeChatWork:AllowedRedirectHosts`），消除开放重定向风险
- `appsettings.json`：新增 `Auth` 节（Secret/TokenExpiryMinutes）与 `WeChatWork:AllowedRedirectHosts`
- `DatabaseScripts/AddWeChatWorkUserIdToUser.sql`（+rollback）：`用户管理` 表加 `WeChatWorkUserId` 列 + 过滤唯一索引（DB-First，须核对中文表名后执行）
- `docs/企业微信工作台免登-前端对接.md`：前端路由守卫/service 骨架 + 企微后台配置步骤

## 验证
- `dotnet build` 通过（0 错误）。原 `bin` 被运行进程 PID 22600 锁定，改用 `bin_verify` 临时输出验证编译。
- 注意：① 用户管理表加列 SQL 须与真实生产库中文表名核对后执行；② 现有账密登录 token 因格式变更全部失效（开发期可接受，上线前需通知）；③ 自动建号默认 Role=User。

## 前端落地后复查（09:58）
- 对照前端计划 `stellar-nebula-tesla.md` 核对接口契约：已对齐——`authorize-url` 返回裸 `{url}`（前端直接读 `.url`）；`login-by-wechatwork` 返回 `ApiResponse<LoginResultDto>`（Data 含 Token/User，PascalCase，前端 toCamelCase）；请求 `{Code}`；两接口均匿名；CORS 已含 localhost:5173。无需改接口逻辑。
- `Program.cs` CORS 来源改为从 `Cors:AllowedOrigins` 读取（默认含 dev 5173/内网），上线追加生产前端域名无需重编译。appsettings.json 新增 `Cors` 节。编译 0 错误通过。
- 仍待办（非代码）：① 执行 `AddWeChatWorkUserIdToUser.sql`（免登运行时依赖该列，否则报错）；② 填 `WeChatWork.CorpId/AgentId/AgentSecret`（缺失后端启动即抛异常）、`Auth.Secret`；③ 生产前端域名加入 `AllowedRedirectHosts` 与 `Cors:AllowedOrigins`。

## 纠正：生产配置走环境变量（用户指出）
- 用户指出"企业微信生产配置不是读环境变量吗"——确认正确。ASP.NET Core 默认合并环境变量并覆盖 appsettings.json（连接串 `ConnectionStrings__DefaultConnection` 已验证生效）。代码 `GetSection("WechatWork").Get<WechatWorkClientOptions>()` 读合并配置，故生产值经 env 注入即可，无需改 appsettings.json。
- 对应环境变量名（双下划线为节分隔，Windows 大小写不敏感）：`WeChatWork__CorpId`、`WeChatWork__AgentId`、`WeChatWork__AgentSecret`、`Auth__Secret`；生产前端域名经 `WeChatWork__AllowedRedirectHosts`、`Cors__AllowedOrigins` 覆盖。appsettings 中同名项仅为本地占位。
- 环境变量审计（10:14）：`WeChatWork__CorpId/AgentId/AgentSecret` 均已设置；`Auth__Secret` 缺失（代码有默认密钥兜底，故后端可启动、免登可跑，但生产不安全，需注入）。另有 `CorpId`/`AgentSecret` 无前缀变量不会被绑定，可忽略。真正剩余：① 注入 `Auth__Secret`；② `WeChatWork__AllowedRedirectHosts` 追加生产前端域名；③ `Cors__AllowedOrigins` 追加生产前端源。
- `Auth__Secret` 已注入：用 RandomNumberGenerator 生成 64 字节 Base64(长度88) 随机密钥，经 `[Environment]::SetEnvironmentVariable(...,"User")` 持久化到用户环境变量(HKCU\Environment)，验证 length=88。（**密钥值不写入任何记忆/文件**。）后端重启后即从该变量读取，不再用默认开发密钥。
- 前端地址澄清（10:22）：用户告知生产前端部署在 `http://192.168.1.110:1001`。核对 `appsettings.json` 后发现该地址已被默认配置覆盖：`AllowedRedirectHosts` 含 `192.168.1.110`（端口不影响 host 校验），`Cors:AllowedOrigins` 已含 `http://192.168.1.110:1001`。无需追加环境变量。

## 数据库脚本已执行（10:04，连接串取自环境变量 ConnectionStrings__DefaultConnection）
- 目标库：`192.168.1.18\SQL2008` → `ycdbnnew`（sa SQL 认证）。只读校验确认 `用户管理` 表存在、列 `WeChatWorkUserId` 不存在。
- sqlcmd 坑（记下来避免再踩）：① `-i` 带盘符/中文路径报 "file D: 拒绝访问"，改用 stdin 重定向 `< file`；② 目标系统代码页为 GBK(936)，UTF-8 脚本经 stdin 被按 GBK 解码导致中文表名乱码（`用户管理`→`鐢ㄦ埛绠＄悊`），需用 Python `open(...,encoding='gbk').write(utf8内容)` 转 GBK 再喂；③ ALTER 与 CREATE INDEX 必须 GO 分两批（同批编译期报列不存在 207），且筛选索引需 `SET QUOTED_IDENTIFIER ON; SET ANSI_NULLS ON;`。
- 修订后的脚本已落地（`DatabaseScripts/AddWeChatWorkUserIdToUser.sql`，含 SET 选项 + 分两批 + 索引存在性守卫，可重入）。
- 验证：列 `WeChatWorkUserId` 与筛选唯一索引 `IX_用户管理_WeChatWorkUserId` 均已存在于 `用户管理` 表。免登运行时依赖已满足。
- 临时文件 `D:\ww_mig.sql` / `D:\ww_mig_gbk.sql` 已清理。

## 企微登录 400 排查 + hosts/白名单调整（11:19）
- 用户在企微工作台点应用报 `请求失败 status 400`。后端代码层面 400 仅来自 `AuthController.LoginByWeChatWork` 的 `dto?.Code` 为空 / 模型绑定失败；企微 API 错误被 `UserService` 捕获后包装成 `Success=false` 的 200，不会直接 400。需看前端 Network 的 Response.Message 与后端控制台日志才能最终定位（日志仅输出到 Console，无文件）。
- 关键配置问题：企微「可信域名」只能填**域名**不能填 IP；用户后台可信域名是 `www.ycdcf.com`（已完成归属认证），但前端实际部署在 `http://192.168.1.110:1001`（IP）。两者 host 不一致会导致 OAuth 静默授权失败。
- 已为测试电脑改 hosts：`C:\Windows\System32\drivers\etc\hosts` 追加 `192.168.1.110 www.ycdcf.com`（备份 `C:\Users\Administrator\hosts.backup.20260807112126`）。hosts 仅本机生效，故要求企微客户端也跑在本机。
- 同步改 `appsettings.json`：`WeChatWork:AllowedRedirectHosts` 追加 `www.ycdcf.com`；`Cors:AllowedOrigins` 追加 `http://www.ycdcf.com:1001`（前端经域名页访问时浏览器 Origin 是域名，CORS 需放行）。配置运行时读取，重启后端即生效，无需重编译。
- 待用户后台手动操作：企微自建应用「应用主页」改为 `http://www.ycdcf.com:1001/#/wechat-login`；重启后端使 `Auth__Secret` + 本次白名单改动生效。

## Network Error 根因 + 监听地址修复（11:25）
- 错误从 `400` 变为 `Network Error`，curl 本机定位：后端仅监听 `127.0.0.1:5246`（`localhost` 通=200，`192.168.1.110:5246` 不通=超时）。前端经 `www.ycdcf.com:1001`(hosts→192.168.1.110) 打开，浏览器发往 `192.168.1.110:5246` 的连接被网络层拒绝 → axios 报 Network Error。
- 已改 `Program.cs`：`builder.WebHost.UseUrls("http://0.0.0.0:5246")` 监听所有网卡。编译 0 错误通过（临时 bin_verify 验证，已清理）。需重启后端生效。
- 企微内置浏览器 F12 无效，无法看 Network Response；需改用本机 curl 或服务器日志排查。
